// The Global Object
 var global = this; // Define a global variable to refer to the global Object

 //Strings have properties and methods:

 var s = "hello world";	// A String
 var word = s.substring(indexOf(" ")+1, s.length); //Use string properties

 var t = "test";  // Start with a string value.
 t.len = 4; // Set a property on it.
 var e = t.len;	// Now query the property.

 